Memberships
-User should see link fot Basic plan on home page
-User should see link for pro plan on home page 
-Basic plan link should take straight to devise plan
-pro plan link should point to custom sign up


Stripe Intergration
* Users should have a stripe customer token in the users table.
* java should prevent pro form from submitting, when user fills it out,
and should send card info to Stripe. Stripe will return a card token.
* java should send user fields and card token to our rails app. Rails app should validate 
the user fields. Rails app will note plan id
*Once validate, rails should send user info (email, plan id) and card token to stripe server and 
Stripe will create a subscription and send back customer token. Rails should save customer token and user info to db.


